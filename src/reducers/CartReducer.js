import {
    
} from './ActionTypes'
const CartReducer = (state, {type, payload}) => {
    switch (type) {
        // case PRODUCT:
        //     return {...state, product:payload}
        default:
            return {...state};
    }
}

export default CartReducer